management.endpoints.web.exposure.include=*
encrypt.key=qixin

#keytool -genkeypair -alias "config-info" -keyalg "RSA" -dname "CN=Web Server,OU=Unit,O=Organization,L=City,S=State,C=US" -keypass qixin456 -keystore "server.jks" -storepass qixin123
# keystore文件的路径
encrypt.key-store.location=server.jks
# alias 指定密钥对的别名，该别名是公开的;
encrypt.key-store.alias=config-info
# storepass 密钥仓库
encrypt.key-store.password=qixin123
# keypass 用来保护所生成密钥对中的私钥
encrypt.key-store.secret=qixin456